-- delete from users where 1=1;
-- insert into users (id, full_name, email, nickname, password, created_at)
-- values (1, 'Alexandre', 'alexandre@dias.com', 'alexandredias', '123', now());
select * from users;